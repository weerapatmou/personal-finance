// Vitest global setup. Currently just imports jest-dom matchers for component
// tests that may be added in later phases; the lib tests don't need it but
// importing here is harmless.
import "@testing-library/jest-dom/vitest";
